import React from 'react'
import './char-card.css'

export default function CharCard({
    url,
    name,
    gender,
    culture,
    born,
    died,
    aliases,//[]
    titles, //[]
    books, //[apis]
    povBooks, //[]
    tvSeries, //[]
    playedBy,//[]
}) {
    return (
        <div>
            
        </div>
    )
}
